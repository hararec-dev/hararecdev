export interface ServiceCardProps {
    icons: React.ReactNode[];
    title: string;
    description: string;
    benefits: string[];
    delay: number;
}

export interface ServiceItem {
    icons: string[];
    title: string;
    description: string;
    benefits: string[];
    delay: number;
}