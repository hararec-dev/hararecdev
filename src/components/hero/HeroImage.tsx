interface HeroImageProps {
    // Define your props here
}

export const HeroImage: React.FC<HeroImageProps> = ({ }) => {
    return (
        <div className='container'>
            <p>HeroImage Component</p>
        </div>
    );
};
