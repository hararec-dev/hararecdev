interface LogoProps {
    // Define your props here
}

export const Logo: React.FC<LogoProps> = ({ }) => {
    return (
        <div className='container'>
            <p>Logo Component</p>
        </div>
    );
};
